# Deliverfile — No Sleep Pro
# https://docs.fastlane.tools/actions/deliver/

# App metadata
app_identifier("thisisvision.AwakeApp")
username("owenmcloughlin1@gmail.com")

# Pricing
price_tier(3) # $2.99 USD

# Categories
primary_category("UTILITIES")
secondary_category("PRODUCTIVITY")

# Age rating (all "None" = 4+)
app_rating_config_path("./fastlane/rating_config.json")

# Submission info
submit_for_review(false)          # Set to true when ready
automatic_release(false)          # Manually release after approval
phased_release(false)

# Skip uploading these if not ready yet
skip_screenshots(false)
skip_metadata(false)

# Force overwrite any existing metadata
force(true)

# Platform
platform("osx")
